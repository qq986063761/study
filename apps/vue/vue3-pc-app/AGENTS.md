# AI 项目指令入口

本项目先继承 `study` 仓库的统一规则，开始处理任务前必须先完整读取并遵守：

[../../AGENTS.md](../../AGENTS.md)

本项目还保留专属规则，按任务读取本目录 `.ai/` 下 `context`、`standards`、`workflows`、`memory` 中的相关文件。专属规则用于补充 Vue 3 项目约定，不得放宽上级通用约束。
