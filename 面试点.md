# html
- 单页面和多页面区别
- 如何提高 SEO

# css
- 盒模型
- 优先级、特指度、ICE
- 内联元素和块元素
- 定位
- 居中
- 浮动
- demo：如何写一个三边带阴影的三角形（canvas、div加伪类）
- demo：如何实现一个正六边形（canvas、div拼接）
- demo：如何让一行三个相等半径的圆，能随所在容器的宽度变化，而自适应（利用 padding 相对自身宽度的百分比）；

# js
- 原型、原型链、继承
- 作用域、上下文
- 闭包（IIFE）
- 异步、同步的区别
- 事件冒泡、捕获
- 模块化（CommonJS、AMD、CMD、ES6、UMD）
- MVC、MVVM
- 本地缓存（localStorage 和 sessionStorage）

# http
- get 和 post请求区别
- 跨域
- 常见响应状态码
- 一次http请求过程

# h5
- 为什么要做适配
- 响应式布局
- 1px border

# jquery
- jquery 和 vue 区别

# vue
- 生命周期函数
- v-show 和 v-if 的区别
- computed 和 watch 的区别
- 为什么 vue 组件中 data 要用函数而不直接使用对象
- 组件通信（props和$emit；provide和inject；$on和$emit）
- keep-alive的作用
- slot作用，有哪些类型的slot（默认、具名、作用域）
- Vue.set、vm.$set 的作用
- Vue.nextTick、vm.$nextTick 的作用
- Vue.extend 的作用
- render和模版的区别
- 虚拟dom（减少dom操作，提高效率）
- v-for 中使用 key 属性的作用（避免默认复用机制的副作用、提高diff查找虚拟节点效率）
- vue 项目中是如何避免样式污染的（style scoped）
- 数据响应原理（Observer、Dep、Watcher三个对象之间依赖收集、通知渲染的过程）

# vue-router
- 配置中 mode 的 hash 和 history 的区别
- 跳转路由的方式

# vuex
state 和 getters
mutations 和 actions
modules

# 解决方案
- 防抖、截流
- 如何优化首屏加载（懒加载、异步加载、cdn资源动态加载）