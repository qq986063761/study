# `.ai` 目录约定

不确定一条规则应该放在哪里时，先判断它回答的是什么问题：

| 问题 | 目录 |
| --- | --- |
| 项目是什么样的？ | `context/` |
| 代码应该怎么写？ | `standards/` |
| 任务应该怎么做？ | `workflows/` |
| 以前为什么这么做？ | `memory/` |

根目录 `.ai/` 只保存与具体技术栈无关、适用于所有子项目的规则。框架、语言或项目专属规则应放在对应子目录中，由该目录的 `AGENTS.md` 继续引用。
